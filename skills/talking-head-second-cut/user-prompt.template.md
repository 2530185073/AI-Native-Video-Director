# 用户消息模板（填空后整段发给模型）

把 `{{…}}` 换成真实数据。词表可整份粘贴 `catalog.md`，或只保留「可用词表」一节。

```
## 视频信息
时长：{{duration_sec}} 秒，共 {{chunk_count}} 个字幕片段。
画幅 1080x1920。构图：{{tight|medium|wide}}。
{{可选：人脸大约在画面高度 X%–Y%；衣着主色；字幕区是否杂乱}}

## 这条视频的节奏预算（参考值，最终由内容决定）
{{例如：信息密度中等。punch 约 3-5 个，zoom 约 2-4 次，broll 约 3-5 张。
hook：片段 1 应有 punch；前 3 秒不要 fullscreen / pip_face。
重要介绍段必须各配至少一张 AI 配图。最后一句要看见人脸。}}

## 需求简报
发布平台：{{抖音/视频号/小红书}}
目标观众：{{…}}
视频目的：{{…}}
风格要求：{{…}}
{{没有就写：无额外要求，由你根据内容判断。}}

## 原始文案
{{整段口播文案}}

## 字幕片段表（id | 开始-结束秒 | 字幕文本）
1 | 0.10-1.80 | {{第一句}}
2 | 1.80-3.40 | {{第二句}}
{{继续，每个片段一行；id 从 1 连续编号}}

## 可用词表（只能从这里选，名称/ID 必须一字不差）
{{粘贴 catalog.md}}

## 输出要求
只输出一个 JSON 对象，不要解释。字段：
- concept、tone、bgm、subtitleStyle、chunks、beats
- 时间只用 chunk id（chunkId / fromChunk / toChunk），不要写秒数
- chunks 必须覆盖上面片段表里每一个 id
- punch.text / highlights 必须来自对应字幕
- 花字 position 用 above_head 或 top，不要用 chest
- 字幕不要开黑色底条
```
